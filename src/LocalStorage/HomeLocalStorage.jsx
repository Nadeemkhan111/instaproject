import React from 'react';
import Game from './Game';
import LocalStorage from './LocalStorage';

const HomeLocalStorage = () => {
    return (
        <div>
            {/* <LocalStorage/> */}\
            <Game/>
        </div>
    );
};

export default HomeLocalStorage;